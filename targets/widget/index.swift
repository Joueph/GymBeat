import WidgetKit
import SwiftUI

@main
struct exportWidgets: WidgetBundle {
    var body: some Widget {
        GymBeatWidgetLiveActivity() // Certifique-se que este nome bate com a struct do Passo 2
    }
}